# REVIEW-WEB-001 — final acceptance evidence reconciliation / 08

Date: 2026-09-12 KST. Scope: documentary evidence reconciliation only. No product suite, build, migration, service, database operation, browser flow, or duplicate Backend/Frontend code review was performed. This task writes only this report and does not edit `ACCEPTANCE.yaml`, `TASKS.yaml`, or `PROJECT_STATE.yaml`.

## Disposition

**Do not accept REVIEW-WEB-001 or Phase 1 yet.** The current authoritative full review, [REVIEW-WEB-001-08.md](REVIEW-WEB-001-08.md), ends in **CHANGES_REQUESTED** for **R08-WEB-002, MAJOR**: the default relative storage root placed the retained screenshot at `C:\Dev\storage\local`, outside the contracted repository storage location. The September 9 [ENV-P1-DB-001-08.md](ENV-P1-DB-001-08.md) acceptance remains valid for the checks it actually accepted—configured authentication, migration-head observation, readiness, retained identity/metadata/bytes/text/filter/CORS readback on the then-effective path—but the later September 11 Web review explicitly qualifies that preliminary environment-only closure for storage placement and host/Compose consistency.

The earlier version of this evidence audit treated ENV acceptance as closing every mandatory Web-review blocker. That conclusion is superseded by the later finding recorded in the current full review. Historical failures and successful later checks must both remain attributed; neither erases the other.

Current shared state is not final acceptance evidence:

- [ACCEPTANCE.yaml](../ACCEPTANCE.yaml) keeps Phase 1 `IN_PROGRESS`, `review_result: null`, and AC-WEB-01..13 `NOT_RUN` with empty evidence.
- [TASKS.yaml](../TASKS.yaml) keeps REVIEW-WEB-001 `IN_PROGRESS` / `review_result: null`, while ENV-P1-DB-001 is `DONE` / `ACCEPTED`.
- [PROJECT_STATE.yaml](../PROJECT_STATE.yaml) calls ENV-P1-DB-001 `RESOLVED` and lists no completion blocker, but also says final Web evidence reconciliation remains in progress.

Those YAML values were read, not changed. The ENV state records the scoped September 9 acceptance; it does not override the later independent Web disposition.

## Evidence authority and integrity boundary

The evidence chain used below is:

- **B:** [BACKEND-WEB-001-03.md](BACKEND-WEB-001-03.md), owner 03. Windows/Linux PostgreSQL suites each report 54 PASS; the real TypeScript-client integration reports 2 PASS. It covers scoped CRUD, Unicode, screenshot upload/storage behavior under isolated absolute storage roots, metadata/content/Expected Strings/filter behavior, and fresh migration/schema checks.
- **F:** [FRONTEND-WEB-001-04.md](FRONTEND-WEB-001-04.md), owner 04. It reports 23 behavior tests, typecheck, production build, two real client/API/PostgreSQL integration tests, and an isolated desktop browser catalog/upload/detail/filter flow PASS.
- **R:** [REVIEW-WEB-001-08.md](REVIEW-WEB-001-08.md), independent reviewer 08, review window September 8–11. It independently executed 54 Backend tests, 23 Frontend tests, typecheck, production build, and two real client integration tests, all PASS. It also independently completed a configured read-side browser check. Its final disposition is CHANGES_REQUESTED for R08-WEB-002; R08-WEB-001 is MINOR/nonblocking.
- **EO:** [ENV-P1-DB-001-03.md](ENV-P1-DB-001-03.md), owner 03. It reports additive migration/current head, normal Web upload/display/filter behavior, and retained readback across a controlled database/API restart without reseeding.
- **EI:** [ENV-P1-DB-001-08.md](ENV-P1-DB-001-08.md), independent reviewer 08. It independently reports PASS for configured authenticated query, head `0002_phase1_domain`, readiness, saved/list/detail identity, original bytes/hash, associations, both Expected Strings routes, exact multilingual text, filters, and CORS. It expressly accepts environment closure only and attributes migration execution, browser interaction, and restart to EO.
- **H:** [REVIEW-WEB-001-01.md](../handoffs/REVIEW-WEB-001-01.md), PM activation and gate instructions. Final review acceptance requires configured environment closure and independent Web review acceptance; PM owns Task/AC/Phase state changes.

The existing 90-file [source manifest](REVIEW-WEB-001-08-source-manifest.json) comparison remains usable only within its stated limit: 90/90 paths existed, 89 hashes matched, and only `backend/tools/verify_default_persistence.py` differed because of the verifier correction accepted by EI. It is an end-of-review snapshot, not a complete repository inventory or retroactive proof of bytes at every earlier execution. No new hash comparison was performed here. The unchanged-manifest result preserves prior regression evidence, but it cannot negate R08-WEB-002 because R states that the storage-root bug was already present in the tested submission and isolated tests supplied absolute temporary storage roots.

## AC-WEB recommendations

These are evidence recommendations for the parent reconciliation. They are not current YAML statuses and are not new executions. `BLOCKED` means the available evidence must not be promoted to PASS until the stated R08-WEB-002 closure is independently verified.

| Criterion | Recommended status | Exact retained evidence and limit |
| --- | --- | --- |
| **AC-WEB-01 Project CRUD** | **PASS** | B reports Project CRUD and restrictive-delete coverage. R independently ran `test_project_crud` and the actual ApiClient integration. F reports real-client CRUD and browser project creation. EI retained the configured project association. R08-WEB-002 does not invalidate database CRUD evidence. |
| **AC-WEB-02 Build CRUD** | **PASS** | B reports scoped Build CRUD, immutable identity, duplicate/concurrency and restrictive-delete checks. R independently ran the parametrized Build CRUD and actual-client integration. F reports client/browser Build creation; EI retained the Build association. |
| **AC-WEB-03 Locale management** | **PASS** | B reports scoped Locale CRUD, canonicalization/validation and uniqueness. R independently exercised scoped Locale behavior and real-client integration. F reports client/browser Locale creation; EI retained the Locale association. |
| **AC-WEB-04 Category management** | **PASS** | B reports scoped Category CRUD and relationship checks. R independently exercised scoped CRUD and integration. F reports client/browser Category creation and dependent selection behavior; EI retained the Category association. |
| **AC-WEB-05 Situation management** | **PASS** | B reports scoped Situation CRUD and Category relationship enforcement. R independently exercised those paths and real-client integration. F reports browser/client Situation creation and mapping workflow; EI retained the Situation association. |
| **AC-WEB-06 String ID multilingual management** | **PASS** | B reports StringKey/StringEntry CRUD, scope uniqueness, empty/missing distinction, and exact Japanese/Korean/emoji/combining/newline/tab readback plus rejection cases. R independently ran those Backend and actual-client cases. F reports translation/mapping UI behavior. EO supplied the configured multilingual baseline; EI independently read back the exact text at both endpoints. Storage placement does not invalidate the database string-management result. |
| **AC-WEB-07 Manual screenshot upload** | **BLOCKED** | B/R/F establish PNG/JPEG validation, multipart/FormData behavior, side-effect handling, component behavior, and owner browser chooser-to-detail success. EO reports normal configured Web upload, and EI independently retained the screenshot ID and bytes/hash. However, R later proves that this configured upload persisted the image outside the contracted repository root. Isolated tests used absolute temporary roots and do not cover the defect. Promote only after R08-WEB-002 correction plus preservation/upload/readback verification on the correct host and Compose paths. |
| **AC-WEB-08 Screenshot metadata** | **PASS** | B and R cover required list/detail fields, associations, MIME, dimensions, bytes/hash and invalid metadata. F reports detail rendering. EO reports configured detail; EI independently verifies saved/list/detail identity, all associations, metadata and image hash. The metadata assertions passed even though AC-WEB-07 remains blocked on physical placement. Correct-root closure must still confirm the existing screenshot remains readable without changing these values. |
| **AC-WEB-09 Expected Strings display** | **PASS** | B/R cover ordered current-catalog behavior, missing versus empty translations, shared resolver endpoints and component display. F reports browser display. EO reports configured display; EI independently verifies exact multilingual text at both Expected Strings endpoints and zero missing values for the retained fixture. Missing/empty cases come from R, not the EI baseline alone. |
| **AC-WEB-10 Project / Build / Locale / Situation filters** | **PASS** | B/R cover scoped AND/time filters, relationship validation, client routing, dependent resets and stale-response protection. F reports browser filter/reset behavior. EO reports the configured filtered list; EI independently returns the one retained screenshot with matching Project/Build/Locale/Category/Situation associations. |
| **AC-WEB-11 Fresh database migrations reproduce domain schema** | **PASS** | B reports fresh PostgreSQL migration roundtrip, model parity and direct FK checks. R independently ran `test_fresh_migration_roundtrip_and_metadata`, the FK test and integration migrations. EO reports additive configured upgrade/current head; EI independently observes `0002_phase1_domain`. This criterion does not require a destructive user-database downgrade. Storage-root remediation is not a schema migration result. |
| **AC-WEB-12 Backend tests PASS** | **PASS** | R independently records Windows PostgreSQL **54/54 PASS**, zero failures/errors/skips, in `reviewer-backend-windows.xml`. B separately reports Windows/Linux 54 PASS each; Linux remains owner evidence. R08-WEB-002 is an uncovered configuration/product-path defect, so the suite result remains PASS while overall review remains CHANGES_REQUESTED. |
| **AC-WEB-13 Frontend build and behavior tests PASS** | **PASS** | R independently records **23/23** behavior tests, `tsc --noEmit`, Next.js 16.3.4 production build, and **2/2** actual TypeScript-client/FastAPI/PostgreSQL integration tests PASS. F separately reports the browser flow. No rerun is required solely to restate these retained results; affected reruns depend on the eventual storage fix. |

Result: **12 criteria are evidence-sufficient for PASS recommendation; AC-WEB-07 remains BLOCKED.** Because every criterion is required and the independent review is CHANGES_REQUESTED, Phase 1 cannot be accepted.

## Final acceptance prerequisites

1. **Resolve R08-WEB-002 in the product/configuration boundary.** Make default relative storage resolve to the contracted repository location for host execution, keep Compose `/app/storage/local` and its repository bind mount consistent, and document the effective path without exposing credentials.
2. **Preserve existing data.** Keep the configured PostgreSQL database/volume, screenshot row/ID, metadata, exact multilingual Expected Strings, and original image bytes/hash. Move or otherwise reconcile the already retained object from the wrong path without reseeding or silently replacing the baseline.
3. **Revalidate the affected configured flow.** With normal root configuration, show authenticated DB identity, current migration head, `/ready` 200, successful manual Web upload to the corrected storage location, list/filter/detail/content/Expected Strings reads, and unchanged retained-baseline readback before and after an ordinary application restart. Attribute owner actions and independent checks separately.
4. **Run only checks affected by the fix.** If the change is configuration/path-only, targeted storage/configuration regressions plus the configured flow are sufficient. If runtime, migration, API contract, or Frontend code changes, rerun the corresponding affected suites and refresh the reviewed-source boundary. The prior 54/23/typecheck/build/2-integration PASS results need no blanket repetition without a relevant change.
5. **Obtain an independent re-review disposition.** Reviewer 08 must verify R08-WEB-002 closure and issue an updated REVIEW-WEB-001 **ACCEPTED** disposition. Findings from the separately assigned Backend/Frontend inspections must also be reconciled; this audit does not pre-empt them.
6. **Then reconcile shared state through the PM-owned gate.** Record AC-WEB-01..13 PASS with evidence, REVIEW-WEB-001 accepted, and only then set Phase 1 to ACCEPTED. Later phases stay inactive until that gate is complete.

R08-WEB-001, the stale root README guidance, remains MINOR and explicitly nonblocking in R. It should be corrected and reviewed against current scripts/runbooks, but it is not a prerequisite for Phase 1 acceptance unless the parent changes that severity or another review establishes a blocking documentation requirement.

No additional mandatory full browser repetition, Linux/clean-install rerun, Docker Frontend image build, mobile/exhaustive accessibility audit, load/power-loss test, or later-phase work is established by the reviewed gate. The required remaining work is the concrete storage-root defect and its preservation/restart verification, plus independent acceptance and state reconciliation.
