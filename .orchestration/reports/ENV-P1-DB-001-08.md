# 2026-09-13 designated final closure — ACCEPTED

**ENV-P1-DB-001 full closure ACCEPTED; R08-WEB-002 RESOLVED.** The corrected instantiated adapter root and every configured DB reference were independently verified; original legacy copy remains intact, original/new objects and live readback match IDs/hash/metadata/Unicode, and final root regression passes. Owner restart/browser evidence is corroborated without changing its attribution. See [designated closure](R08-WEB-002-closure-08.md). This supersedes the full-closure hold below while preserving the reason for that hold and all dated results. PM owns issue/task/Phase reconciliation.

---

# Historical 2026-09-12 correction — full closure withheld

**Current disposition: CHANGES_REQUESTED for full environment closure pending R08-WEB-002 correction and storage preservation verification.** The 09-09 DB authentication/readiness/head and same-path restart readback PASS observations below remain valid. Their environment-only ACCEPTED conclusion and recommended immediate closure are superseded, not their executed results.

The later independent review found `backend/app/storage/local.py:11` anchors relative storage at the repository's parent. The retained synthetic image is actually under `C:\Dev\storage\local`, absent at the required repository `storage/local`; its SHA-256 matches the original baseline. Repeated readback therefore did not establish the reported repository-relative placement. This is a MAJOR product defect tracked as R08-WEB-002 in [the final Web review](REVIEW-WEB-001-08.md), not a new authentication failure.

PM should retain historical DB failure/success attribution, keep full ENV closure pending correct-root/existing-object verification, and return the product fix to Backend 03. Do not reset credentials, volumes or baseline records. After the scoped correction, verify actual resolved root and original IDs/hash/metadata/Unicode after the preservation procedure and ordinary restart. All remaining status changes are PM-owned.

---

# Historical 2026-09-09 ENV-P1-DB-001 independent environment review

Date: 2026-09-09. Reviewer: 08. Result: **ACCEPTED — environment closure only**.

The submitted configured-deployment evidence and independently reproduced checks satisfy ENV-P1-DB-001 closure. This does not accept REVIEW-WEB-001, any complete AC-WEB criterion, or Phase 1. Parent/PM owns state reconciliation; no state files were changed.

## Inputs and attributed owner evidence

Reviewed reports/ENV-P1-DB-001-03.md, handoffs/ENV-P1-DB-001-03.md, the original closure requirements in handoffs/REVIEW-WEB-001-01.md, the records manifest, both verification tools, application Settings and migration revision declarations.

Owner 03 reports additive `alembic upgrade head` and `alembic current` success, prior normal Web-form upload/catalog/Expected Strings/filter evidence, and successful corrected-verifier readbacks before and after a controlled DB/API restart. Reported API PID changed 52616 -> 27168; the same PostgreSQL container's StartedAt changed from 2026-09-09T14:29:58.868135256Z to 2026-09-09T14:31:19.775684641Z. The existing DB volume and root-configured storage/local were retained without reseeding. These restart, migration-execution and browser observations are owner-provided evidence, not actions independently repeated by this reviewer. The original authentication failure remains historical; its cause is not established by present success.

## Independently reproduced evidence

Commands executed from C:\Dev\qa-visual-automation, with bytecode writes disabled:

| Command | Result |
| --- | --- |
| `.\.venv\Scripts\python.exe -B backend/tools/verify_default_persistence.py verify` | PASS, exit 0: readiness, one filtered screenshot, saved/list/detail screenshot identity, project/build/locale/category/situation associations, original bytes/hash, exact multilingual text at both Expected Strings endpoints, zero missing translations, browser-origin CORS preflight. |
| `.\.venv\Scripts\python.exe -B backend/tools/check_default_environment.py` | PASS, exit 0: authenticated query `[1, "qa_visual", "qa_visual"]` against 127.0.0.1:5433; migration `0002_phase1_domain`, matching the repository head declarations. |

Probe independently reported the running container's volume qa-visual-automation_postgres_data at /var/lib/postgresql/data, all three app/Compose/container/dotenv password comparisons true, and no relevant process override keys. No credentials were printed. This probe reads configuration/container metadata and queries the DB; it does not itself prove the API process configuration. That linkage is supported by the owner's launch/restart account and successful API readback of the retained baseline.

Confirmed screenshot ID: `383d49f8-1783-4910-a567-dcd15992c253`.
Confirmed original image SHA-256: `ebfa933afb0bfbe51ae0e2eb059cb58b4dfd22b6fcb67048450257b78b194135`.
Manifest SHA-256 before and after reviewer checks: `0ab5f214eac550f4da5924a550d49f3c9cf3b16aebc9b0ba02c421cf5ef8ad5b` (unchanged).

Initial sandbox attempts failed to launch the project Python runtime; neither was counted as a product failure or PASS. Authorized escalated retries produced the successful results above. No services were started or restarted, migrations applied, seed/baseline modes invoked, or application records modified by this reviewer.

## Verifier finding disposition

Prior baseline-ID overwrite finding: **RESOLVED**. In verify mode the corrected implementation requires `shot['id'] == detail['id'] == data['screenshot_id']` and performs no manifest write. Baseline capture is an explicit separate mode that refuses an existing screenshot ID; unknown modes fail. Static inspection and the unchanged manifest hash support read-only verification. No remaining concrete verifier defect blocking this scoped closure was found. The tool still relies on the documented local API origin and external restart evidence; its PASS alone is not a restart test or full Web acceptance.

## Recommended parent state reconciliation

- ENV-P1-DB-001: READY_FOR_REVIEW -> DONE; review_result -> ACCEPTED; review_requested -> false; environment_issue_status OPEN -> RESOLVED. Attach this report as independent closure evidence.
- PROJECT_STATE environment issue: OPEN -> RESOLVED. Remove only ENV-P1-DB-001 from required_completion_blockers and REVIEW-WEB-001.completion_blockers. Reconcile current environment limitations and the failed-probe entry with successful revalidation, preserving the historical FAIL and its attribution.
- Do not promote Backend/Frontend submissions, overall web review, or AC-WEB criteria solely from this closure. Keep Phase 1 IN_PROGRESS and its review_result null until all required criteria and the separate independent web review satisfy their gates. No later-phase activation.

Only this review report was written. No full Phase 1 acceptance is claimed.
