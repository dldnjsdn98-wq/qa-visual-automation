# ENV-P1-DB-001 configured persistence revalidation

Date: 2026-09-09. Owner result: PASS / READY_FOR_REVIEW; independent environment closure requested. This is not full Phase 1 acceptance.

## Environment and preservation

On resume Docker Desktop, API and Web were stopped. Started Docker Desktop and the existing `qa-visual-automation-postgres-1` container, then the normal root-configured API (127.0.0.1:8001) and built Web (127.0.0.1:3001). No seed rerun, credential changes, volume replacement, deletion, downgrade or unrelated-service startup in this continuation.

`backend/tools/check_default_environment.py` authenticated with effective application settings: SELECT 1/current_database/current_user = [1, qa_visual, qa_visual], host 127.0.0.1, port 5433. App/Compose/container/root dotenv password comparisons all true; no relevant process overrides. Credentials are not included. Prior owner authentication failure remains historical; its original cause cannot be established from this continuation. The configured failure is no longer reproducible.

`python -m alembic upgrade head` succeeded; `python -m alembic current` returned `0002_phase1_domain (head)`. Same existing volume `qa-visual-automation_postgres_data` mounted at `/var/lib/postgresql/data`. API launched without storage overrides; configured storage remains `storage/local` from repository root.

## Prior live browser evidence

Previous execution in this task seeded uniquely named project `env-p1-db-7c0c2f92` through the default API, including Build/Locale/Category/Situation, StringKey, multilingual StringEntry and expected mapping. Browser selected those catalog values, uploaded synthetic `env-p1-db-001.png` using the Web form, and showed the detail/Expected Strings and filtered list. Image loaded at 128 x 72; original length 243 bytes. This is retained prior execution evidence, not a new browser run on 2026-09-09.

Baseline IDs are retained in `ENV-P1-DB-001-records.json`. Screenshot: `383d49f8-1783-4910-a567-dcd15992c253`; project: `41ee9635-80f5-4dfd-8c7f-25c4be181f47`. Exact text: `한국어 日本語 中文 العربية 😀 é` (last sequence U+0065 U+0301).

## Controlled restart and readback

1. Restored configured API, then ran `python backend/tools/verify_default_persistence.py verify`: PASS against original manifest.
2. Recorded API PID 52616 and container ID `6ebd78cb5629b910fc37eba42561d9fec86b24e7cb614d7e86d4932e11a0c3f3`, StartedAt `2026-09-09T14:29:58.868135256Z`.
3. Verified listener command line is the intended uvicorn app, stopped that API process, ran `docker restart qa-visual-automation-postgres-1`, and restarted the same API using root settings/storage.
4. Repeated verifier: PASS. New API PID 27168, same container ID, StartedAt `2026-09-09T14:31:19.775684641Z`. Repeated authenticated DB probe: PASS, same identity, volume, credential comparisons and migration head. Web HTTP GET returned 200.

Both readbacks verified readiness, filtered list (one original screenshot), original list/detail IDs, project/build/locale/category/situation associations, content bytes against stored file_hash and baseline SHA-256, exact Unicode from both Expected Strings endpoints, zero missing translations, and permitted browser-origin CORS preflight.

Original and post-restart SHA-256: `ebfa933afb0bfbe51ae0e2eb059cb58b4dfd22b6fcb67048450257b78b194135`.

## Verification tool fix and limits

Independent subagent inspection found verification previously overwrote the baseline screenshot ID. Fixed `backend/tools/verify_default_persistence.py`: verify now requires original ID equality and never writes the manifest; first baseline capture is a separate explicit mode and refuses overwrite. Unknown modes fail. Both successful readbacks used the corrected verifier. Seed is intentionally not rerun on a populated environment.

No application code, dependency or contract change in this continuation; no broad suite rerun needed for environment/tool-only work. Existing unrelated working changes are retained. `git diff --check` passed (line-ending notices only). Full Web AC review and Phase 1 acceptance remain separate. No commit/push.
