# ARCH-001 evidence report — owner 02 — 2026-09-05

## Result and scope

Architect specification complete and submitted for independent review. Four required design documents replace bootstrap proposals. No Backend/Frontend, runtime dependencies, migrations or real QA data modified. User explicitly authorized recording actual task status and decisions; ARCH-001 is READY_FOR_REVIEW and dispatch REVIEW_REQUESTED. Acceptance and downstream activation are unchanged.

## Executed checks

Run from project root using README-compatible project .venv:

```powershell
.\.venv\Scripts\python.exe --version
.\.venv\Scripts\python.exe docs/architecture/check_contract.py
```

Observed results:

```text
Python 3.12.14
PASS: 4 documents; 3 local links; 4 JSON examples
PASS: example scopes, missing translation semantics, hash shape and content URL
PASS: orchestration YAML, review-pending state and unchanged downstream gates
NOT_RUN: independent AC-ARCH-01 review, product API/DB/storage/Frontend tests
```

The static checker parses all four JSON examples, checks expected/missing counts and null semantics, UUID scope consistency between upload and response, SHA-256 shape (not actual digest), scoped content URL, local document links, balanced fences, absence of machine executable paths, YAML parsing/unique task IDs, no self-acceptance and unchanged downstream/phase gates. It deliberately checks this submission state; it is not a permanent CI gate after PM acceptance. These are documentation consistency checks, not executable API validation or independent design review.

Other executed inspection: `rg --files` for supplied task documents; `Get-Content -Raw` on role prompt, four required state files, README, bootstrap architecture, PM handoff/template, reviewer prompt, pyproject, Compose, Backend shell/config and placeholder environment file. No secret .env read. `git status --short` showed pre-existing untracked bootstrap tree. `git branch --show-current` returned main; `git rev-parse --verify HEAD` reported 'Needed a single revision', expected for the existing unborn repository (no commit). An initial apply_patch attempt rejected delete/add of the same file as duplicate targets before changing any file; corrected with direct scoped file writes. This was an authoring-tool failure, not a product test failure.

## AC-ARCH-01 author coverage inspection

| Required dimension | Submitted evidence / reviewer focus |
| --- | --- |
| Domain cardinality | domain-model.md tables/relationships: Project root, Situation/Category, Build-specific translations and expected mappings |
| Uniqueness and identity | Stable StringKey, translation tuple, immutable IDs, uniqueness and restrictive deletes |
| Scoped API | api-contract.md path ownership, reference validation, models and complete CRUD route table |
| Error contract | Error envelope, typed status codes, field paths, upload and unknown-resource behavior |
| Storage | data-flow.md interface, validation bounds, publication/commit failure matrix, exclusive reconciliation |
| Migration design | overview.md preserved baseline, additive migration order, isolated PostgreSQL checks and future expansion |
| Phase extensions | domain-model.md receipts/OCR/verification/automation entities; API compatibility and queue separation |
| Environment | overview.md retains project .venv, npm.cmd, README ports and local/container DB addresses |

Author inspection found these dimensions explicitly specified. This table is a submission index, not an independent PASS for AC-ARCH-01.

## NOT_RUN and acceptance limits

- AC-ARCH-01 independent review: NOT_RUN; role 08 has not reviewed this submission.
- AC-WEB-01..13 and domain migration, API behavior, storage faults, Frontend build/behavior: NOT_RUN; architecture-only work, product implementation remains assigned to roles 03/04 behind the acceptance gate.
- Bootstrap test suite: NOT_RUN because it cannot validate this design and no product code changed.
- Phase 2+ execution: NOT_RUN and not activated.
- No branches/commits created, no push, no messages sent to other tasks and no sub-agents spawned.

## Review handoff

PM should coordinate role 08 review of the four documents and canonical ../handoffs/ARCH-001-02.md, obtain ACCEPTED or CHANGES_REQUESTED and update AC-ARCH-01 with review evidence. Only after acceptance may both web tasks be promoted. Existing REVIEW-WEB-001 remains blocked until both web implementations submit evidence. Remaining risks and concrete implementation checks are listed in overview.md/data-flow.md and the canonical handoff.
