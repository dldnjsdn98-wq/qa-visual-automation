# PM01 Linux L1 single capture activation

Task BACKEND-OCR-001 / P3-RUNTIME-001; owner03; root C:/Dev/qa-visual-automation.
PM matched final build manifest ae89f4e4610c7762177685f9b2ca2a43b3c1c9cfd195553b9cdb80985b721443 and all20 artifact sizes/hashes, read exact Mill input gate and unchanged capture independent review. Source application, fixed inputs, single successful new build and immutable image prerequisites now satisfied. This activates only the exact bounded Linux capture below; previous prep-only documents remain historical.

## Exact one-shot scope
Use already approved interpreter with -E -B, no -O/-OO, and frozen .orchestration/reports/BACKEND-OCR-001-native-apply-linux-capture-03.py (SHA256 d0a8e756cdd4cafc394d12a68e527632365aeaf43f6d9ca8cc063763db21d7fd):

    python -E -B .orchestration/reports/BACKEND-OCR-001-native-apply-linux-capture-03.py host --image-id sha256:a1337c5556ab00f01dac45075f6bbf71ba9198c1b179a83d0210e5879a426d0a --source-pins .orchestration/reports/BACKEND-OCR-001-linux-source-pins-05.json --pins-sha256 d92e20493eb42a3d4d2702b17c3d5559c17cad78e31b364387039e649146f2bc --evidence .orchestration/reports/BACKEND-OCR-001-native-apply-linux-evidence-03-06

Confirm fresh output path without modifying existing evidence. Recheck script/input/current raw+LF identities before invocation. Drift blocks execution; no normalization, repin or code edit. Ordinary tool approval boundary applies; denial ends this invocation path, no retry or workaround.

Only selected node tests/backend/test_ocr_containment.py::test_native_pressure_and_owned_tree_cleanup[shortpeak]. Unchanged script verifies image-internal raw+LF before pytest. Retain exact resource/network/read-only root, bounded observer and owned-container lifecycle from reviewed plan. Fixed512MiB cap,320+224MiB pressure,64..160MiB headroom and strict250ms oracle; no tuning, L2 substitution or false t1. One create/start only, terminal facts before exact owned cleanup. No broad cleanup, no force deletion; ambiguity or cleanup failure means unresolved, not PASS.

Capture outer exact command/stdout/stderr/exit and artifact hashes even on failure. Preserve source stability, inner inputs, JUnit when present, terminal/OOM facts, ownership and cleanup outcome. Missing image equality, observer failure, SKIP, forced stop or incomplete timing is unproved; never infer qualification. No automatic second run, rebuild, dependency change, DB/model access, broad tests or Windows hook/probe. Windows count3 and historical cleanup2 unchanged. Phase3 AC all NOT_RUN,04/08/production admission held.

## Delegation and completion
Owner03 controls single execution (high Sol/high requested for bounded native capture coordination; existing parent model retained if not safely adjustable). Reuse Mill Sol/high for independent resulting evidence review, Boole Terra/high for artifact/hash packaging only. Actual model unverified unless attested. Do not duplicate the native run in a subagent. Deliver own report/handoff/manifest with explicit PASS_SCOPED_L1 or exact unresolved/failed disposition, executed/not-run split and remaining gates. PM evaluates downstream activation only after final evidence review. No commit/push/deploy/user-data deletion/security changes.
