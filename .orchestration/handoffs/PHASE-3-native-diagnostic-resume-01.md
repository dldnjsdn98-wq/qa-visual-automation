# P3-RUNTIME-001 native diagnostic resumption / PM01

Authorization: coordinator relayed explicit user request to reassign and continue necessary work. This activates bounded diagnostic/correction work under existing BACKEND-OCR-001 ownership, not host-security changes or a waiver. Preserve final local279PASS15SKIP,103-source and50-artifact checkpoint and all failures. No new app task; recover prior sidecar results and reuse existing agents first. Parent03 requested Astra/medium; actual runtime model unverified.

## Windows lane

Highest complexity: Astra/medium for native process/Job identity and containment correctness. Goal: explain two-live-PID observation under ActiveProcessLimit1 without relaxing it. First read existing captures/source/interpreter/venv redirector evidence and identify which facts are absent. Recover actual launched executable identity and parent/child PID relationship, Job handle/accounting, startup ordering and inheritance hypotheses with discriminating evidence. Do not infer from intended executable name or static PE identity alone. A materially changed bounded diagnostic is authorized after documented read-only root-cause analysis and review; do not repeat the unchanged failed probe. Limit inspection and process cleanup to the test-owned workload. Existing source/test claims remain authoritative. Source changes require root-cause rationale and focused regression. Pressure/timeout/cleanup/real OCR only after prerequisites. Preserve native same-cause2 and separate prior cleanup2 counts; a further same-cause correction/review failure pauses that lane for reassessment, even if diagnostic framing changes. New evidence cannot overwrite old captures.

## Linux lane

Medium complexity: Sol/medium for read-only permission-context/capture analysis; use Sol/high if tool/runtime boundary uncertainty warrants it, not because permissions imply model weakness. Read linux-diagnostic-build-01 and available historical execution/approval records; distinguish sandbox denial, tool approval rejection and host filesystem denial. Identify precise denied config/buildx targets and minimal ordinary approval action/command. No repeated denied build, DOCKER_CONFIG relocation, different identity, ACL/security changes or bypass. If an ordinary approval request is necessary and supported by the analyzed context, present the concrete original-scope operation via the product approval mechanism; approval remains user's decision. Unclear scope or sustained denial blocks only Linux; continue Windows. Do not claim previous success or saved unrelated command prefixes authorize this command. Do not expose Docker config or credential contents.

When genuinely available and authorized, freeze exact source then build image, run change-relevant native pressure/timeout/cleanup, then actual qualified OCR. No old-image substitution; no repetition of completed279-test suite without changed-risk justification. Test execution capture/validation Sol/high, simple evidence index Terra/high. Parent integrates disjoint evidence/files; no duplicate writers.

## Completion and gates

Submit new diagnostic findings, precise authorization requirement if any, proposed/reviewed corrections and actual command/environment/result/source/artifact hashes. Separate skipped/unexecuted/error/approval pending. Existing immutable Worker42/profile/dependency/DB authority/original preservation rules remain. API/schema expansion, model reacquisition, commit/push/deploy and shared DB reset forbidden. Production admission,04 affected Web and08 final review stay blocked until Windows/Linux conformance and matching actual runtime evidence.02 receives a task only for a genuinely new contract interpretation. PM records delivery versus actual active execution separately.
