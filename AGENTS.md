# qa visual 공통 작업 운영 규칙

이 지침은 프로젝트 전체와 프로젝트에서 작업하는 모든 역할 스레드 및 서브에이전트에 적용한다.

이전에 요청한 작업을 이어서 진행한다.

기존에 합의한 요구사항과 현재 스레드의 역할·작업 범위를 유지한다.
그 범위에서 필요한 분석, 구현, 검토, 관련 테스트는
매번 “진행할까요?”라고 묻지 말고 이어서 수행한다.

되돌릴 수 있는 사소한 구현 선택은 기존 코드와 프로젝트 규칙에
맞춰 판단하고, 작업 결과에 기록한다.
다른 스레드가 수정한 내용은 임의로 되돌리지 않는다.

원격 push, 배포, 사용자 데이터 삭제, DB 초기화,
계정·보안 설정 변경은 이번 작업에서 하지 않는다.
요구사항을 크게 바꾸거나 담당 범위를 넘어야 한다면 확인한다.

권한이 차단된 작업은 우회하거나 같은 요청을 반복하지 않는다.
차단 이유를 기록한 뒤 독립적으로 가능한 작업을 계속한다.

마지막에 변경사항, 실제 실행한 테스트와 결과,
미해결 문제를 정리한다. 실행하지 않은 테스트는 통과로 표시하지 않는다.

기존 역할별 지침, 파일 소유권, 단계별 독립 검토 및 승인 기준,
사용자가 지정한 난이도별 서브에이전트 배정 규칙은 유지한다.
이 지침은 기존 내부 검토를 생략하거나 대기 중인 후속 단계를 임의로 활성화하는 권한이 아니다.
